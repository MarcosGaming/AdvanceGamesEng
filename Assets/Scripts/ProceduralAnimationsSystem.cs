using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ProceduralAnimationsSystem : MonoBehaviour
{
    [SerializeField] Transform target;  // Transform that the entity is going to follow

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
